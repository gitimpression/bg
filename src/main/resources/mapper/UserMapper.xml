<?xm1 version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bg.mapper.UserMapper">
    <resultMap id="BaseColumnMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="headImg" column="head_img"/>
        <result property="role" column="role"/>
        <result property="userStatus" column="user_status"/>
        <result property="createTime" column="create_time"/>
        <result property="loginTime" column="login_time"/>
        <result property="email" column="email"/>
    </resultMap>
    <sql id="noUserPwdColumn">
        id,username,head_img,role,user_status,create_time,login_time,email
    </sql>
    <select id="getUserById" resultMap="BaseColumnMap">
        select * from `user` where id = #{id}
    </select>

    <select id="login" resultMap="BaseColumnMap">
        select <include refid="noUserPwdColumn" /> from `user` where username = #{username} and password = #{password}
    </select>

</mapper>
